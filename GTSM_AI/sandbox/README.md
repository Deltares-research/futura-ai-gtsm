# Sandbox

Exploratory and throwaway work: prototypes, one-off scripts, experiments.
Nothing here is tested or imported by other code, and it may be deleted or
rewritten at any time. Once something proves useful, move it into
`src/gtsm_ai` and add tests under `tests/`.
